你是 OpenClaw Bot 项目的首席产品官（CPO 级别），正在执行夜间自动文件治理与文档审计。这是今晚审计的最后一个阶段。

【重要】直接开始执行审计，不要只做计划。发现问题立即修复，修复后验证。

【项目背景】
- 7-Bot Telegram 多智能体系统
- 项目规范: AGENTS.md（必读，特别是第6/9节的文档规则）
- 文档目录结构:
  docs/
  ├── PROJECT_MAP.md           ← 项目全景
  ├── CHANGELOG.md             ← 变更日志
  ├── status/HEALTH.md         ← 系统健康 + Bug + 技术债
  ├── status/HANDOFF.md        ← 会话交接
  ├── registries/              ← 模块/命令/依赖/API 注册表
  ├── guides/                  ← 操作指南
  ├── architecture/            ← 架构文档
  └── sop/                     ← 开发规范

【准备工作】
1. 读取 AGENTS.md 第6/9/10节了解文档规范
2. 读取 docs/status/HANDOFF.md 了解今晚所有阶段的审计进度
3. 读取 docs/CHANGELOG.md 最近 10 条了解变更历史
4. 读取 docs/status/HEALTH.md 了解当前问题状态

【本阶段任务：文件治理与文档审计】

A. 冗余文件清理
- 搜索项目中所有 .md 文件，检查 docs/ 以外是否有不该存在的 .md
  （允许: AGENTS.md, README.md 在根目录；Cargo 项目中的标准 .md）
- 搜索空文件、零字节文件
- 搜索重复文件（内容相同但名称不同）
- 搜索过时的备份文件（.bak, .old, .backup）
- 搜索临时文件（.tmp, ~, .swp）
- 清理找到的冗余文件（先确认不影响功能）

B. 文档命名规范检查
- 检查 docs/ 下所有文件是否符合命名规范:
  - 大写_下划线.md（如 PROJECT_MAP.md）
  - 禁止: 小写命名、中文文件名、空格
- 不符合规范的文件 → 重命名并更新所有引用

C. 注册表完整性审计
对照实际代码检查以下注册表:

1. MODULE_REGISTRY.md — 每个 .py 模块是否都已注册
   - 读取 packages/clawbot/src/ 下所有 .py 文件列表
   - 与注册表对比，补全缺失的模块
   - 移除注册表中已删除的模块

2. COMMAND_REGISTRY.md — 每个 Bot 命令是否都已注册
   - 搜索所有 @handler/@command 装饰器
   - 搜索所有 InlineKeyboardButton 按钮
   - 与注册表对比，补全缺失

3. DEPENDENCY_MAP.md — 每个 pip/npm/cargo 依赖是否已记录
   - 读取 requirements.txt + package.json + Cargo.toml
   - 与注册表对比

4. API_POOL_REGISTRY.md — 每个外部 API/LLM 调用是否已记录
   - 搜索所有 API Key 环境变量引用
   - 搜索所有 LiteLLM 模型配置
   - 与注册表对比

D. HEALTH.md 审计
- 检查所有已标记的 Bug 是否仍然存在（验证修复状态）
- 已修复的 Bug → 标记为已关闭并注明修复日期
- 检查严重度是否准确（已降级的问题调低级别）
- 检查是否有漏登记的问题（根据今晚审计发现）

E. CHANGELOG.md 审计
- 检查今晚所有阶段的修复是否都已记录
- 检查格式是否符合 AGENTS.md 第7节的规范
- 补全遗漏的记录

F. 项目全景文档 (PROJECT_MAP.md) 校准
- 对比实际代码结构与 PROJECT_MAP.md 的描述
- 更新过时的模块描述
- 补充新增的模块/功能
- 确保技术栈描述准确

G. 环境变量完整性审计
- 扫描所有 os.getenv() / os.environ 调用，收集完整的环境变量列表
- 检查是否存在 .env.example 或环境变量文档
- 如不存在，创建 .env.example 列出所有必需的环境变量及说明
- 检查每个环境变量是否有默认值或缺失时的报错提示

H. Git Hooks / Pre-commit 配置审计
- 检查 .pre-commit-config.yaml 是否存在且配置合理
- 检查 hooks 是否覆盖: lint / format / type-check / security-scan
- 检查 hooks 是否与 CI 配置一致（避免本地通过但CI失败）
- 如配置过时或不完整，更新配置

I. HANDOFF.md — 写入今晚审计总结
这是最后一个阶段，需要写入完整的今晚审计总结:
```markdown
## [YYYY-MM-DD HH:MM] 夜间自动审计总结

### 本次完成了什么
[列出今晚 6 个阶段各自完成的工作]

### 未完成的工作
[如有未完成的审计项，列出]

### 需要注意的坑
[只有涉及 UI 审美（颜色/图标/布局好不好看）的问题才列在这里，技术问题全部自主解决]

### 当前系统状态
- 后端测试: X/Y passed
- 前端类型检查: 结果
- Rust clippy: 结果
- 已知问题数: N (🔴 X / 🟠 Y / 🟡 Z)
```

【修复规范】
- 文档更新不需要跑测试
- 每完成一个修复: git add -A && git commit -m "[文档] 描述"
- 注册表更新要精确，不要遗漏也不要冗余
- HANDOFF.md 只保留最近 5 条，删除更旧的

【最终步骤】
1. 运行一次完整测试作为最终状态确认
2. 确认所有文档已更新
3. git add -A && git commit -m "[审计] 夜间自动审计完成 — YYYY-MM-DD"
4. 输出今晚审计的完整摘要
