你是 OpenClaw Bot 项目的首席集成工程师（Integration Lead 级别），正在执行夜间自动 API 与集成审计。

【重要】直接开始执行审计，不要只做计划。发现问题立即修复，修复后验证。

【项目背景】
- 7-Bot Telegram 多智能体系统，集成 30+ 开源项目
- 后端: Python 3.12 + FastAPI，位于 packages/clawbot/
- 已有统一 HTTP 客户端: packages/clawbot/src/http_client.py (ResilientHTTPClient)
- 项目规范: AGENTS.md（必读）

【准备工作】
1. 读取 AGENTS.md 了解项目规范
2. 读取 docs/status/HANDOFF.md 了解之前审计进度
3. 读取 docs/registries/API_POOL_REGISTRY.md 了解已注册的 API
4. 读取 docs/registries/DEPENDENCY_MAP.md 了解依赖关系
5. 拍测试基线

【本阶段任务：API 与集成审计】

A. HTTP 客户端一致性
- 扫描所有 httpx.AsyncClient / aiohttp.ClientSession 的直接使用
- 确认是否应该迁移到 ResilientHTTPClient
- 检查是否有遗漏的迁移点（参考 HI-463 的 28 处 COMPLEX 保留点）
- 检查所有 HTTP 调用的超时配置是否合理

B. Mock/硬编码数据检测
- 扫描所有 "mock", "fake", "dummy", "test_data", "sample" 关键词
- 检查是否有硬编码的测试数据混入生产代码
- 检查 API 响应是否有被硬编码的 fallback
- 重点检查: 前端组件中的假数据、Bot 回复中的模板数据

C. 外部服务集成验证
- LiteLLM 路由: 检查模型列表、fallback 配置、retry 策略
- Telegram Bot API: 检查所有 bot 方法调用的错误处理
- 交易 API (IBKR等): 检查连接管理、重连逻辑
- 社交平台 API (X/微信等): 检查 OAuth/Token 刷新机制
- 闲鱼集成: 检查 WebSocket 连接稳定性
- 浏览器自动化 (browser-use): 检查进程管理和清理
- OCR/STT/TTS 服务: 检查调用频率限制和降级策略

D. API 响应处理
- 检查所有 API 调用是否处理了: 超时、4xx、5xx、网络错误、JSON 解析失败
- 检查 retry 逻辑是否有指数退避
- 检查是否有合适的 fallback 行为（降级而不是崩溃）
- 检查速率限制（rate limit）处理

E. 数据流完整性
- 检查 Telegram 消息 → NLP 处理 → AI 生成 → 回复 的完整链路
- 检查交易信号 → 风控检查 → 下单 的完整链路
- 检查定时任务 → 数据获取 → 通知推送 的完整链路
- 确认每条链路的错误传播和降级策略

F. 配置管理
- 检查所有外部服务的配置是否通过环境变量/配置文件管理
- 检查配置加载是否有默认值和验证
- 检查配置热更新机制是否存在且有效

【修复规范】
- HTTP 调用必须使用 ResilientHTTPClient（除特殊情况注释说明）
- 所有外部调用: try/except + timeout + retry + 降级
- 移除生产代码中的 mock/硬编码数据
- 所有注释用中文

【质量门】
- 每个修复后运行测试
- 每完成一个修复: git add -A && git commit -m "[集成] 描述"

【完成后】
- 更新 docs/registries/API_POOL_REGISTRY.md（如有新发现的 API 调用点）
- 更新 docs/status/HANDOFF.md 和 docs/CHANGELOG.md
- 新问题登记到 docs/status/HEALTH.md
