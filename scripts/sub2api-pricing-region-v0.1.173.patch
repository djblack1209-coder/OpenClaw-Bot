diff --git a/backend/internal/repository/account_repo.go b/backend/internal/repository/account_repo.go
index 010442e..65e5891 100644
--- a/backend/internal/repository/account_repo.go
+++ b/backend/internal/repository/account_repo.go
@@ -15,6 +15,7 @@ import (
 	"database/sql"
 	"encoding/json"
 	"errors"
+	"fmt"
 	"strconv"
 	"strings"
 	"time"
@@ -2712,9 +2713,93 @@ func (r *accountRepository) updateUpstreamBillingProbeSnapshotInTx(
 	if affected == 0 {
 		return service.ErrUpstreamBillingProbeIdentityChanged
 	}
+	if rateMultiplier != nil {
+		if err := syncUpstreamBillingTextGroupRates(ctx, client, account, *rateMultiplier); err != nil {
+			return err
+		}
+	}
 	return enqueueSchedulerOutbox(ctx, client, service.SchedulerOutboxEventAccountChanged, &account.ID, nil, nil)
 }
 
+// syncUpstreamBillingTextGroupRates keeps the single text group associated with
+// a rate-sync-enabled account at the documented account-rate + 0.05 contract.
+// The account-level switch is the explicit managed-price opt-in, so an existing
+// group drift must converge on the next successful probe. User-specific
+// overrides and ambiguous/image bindings remain excluded.
+func syncUpstreamBillingTextGroupRates(ctx context.Context, client *dbent.Client, account *service.Account, nextAccountRate float64) error {
+	if account == nil || nextAccountRate <= 0 {
+		return nil
+	}
+	rows, err := client.QueryContext(ctx, `
+		SELECT g.id, g.rate_multiplier
+		FROM groups g
+		JOIN account_groups ag ON ag.group_id = g.id
+		WHERE ag.account_id = $1
+		  AND g.platform = $2
+		  AND g.status = 'active'
+		  AND COALESCE(g.allow_image_generation, false) = false
+		  AND NOT EXISTS (
+			SELECT 1 FROM user_group_rate_multipliers ugr
+			WHERE ugr.group_id = g.id
+			  AND (ugr.rate_multiplier IS NOT NULL OR ugr.rpm_override IS NOT NULL)
+		  )
+		  AND g.deleted_at IS NULL
+		FOR UPDATE`, account.ID, account.Platform)
+	if err != nil {
+		return err
+	}
+	defer func() { _ = rows.Close() }()
+	type groupRate struct {
+		id   int64
+		rate float64
+	}
+	groups := make([]groupRate, 0, 1)
+	for rows.Next() {
+		var item groupRate
+		if err := rows.Scan(&item.id, &item.rate); err != nil {
+			return err
+		}
+		groups = append(groups, item)
+	}
+	if err := rows.Err(); err != nil {
+		return err
+	}
+	if len(groups) != 1 {
+		return nil
+	}
+	newGroupRate := nextAccountRate + 0.05
+	result, err := client.ExecContext(ctx, `
+		UPDATE groups SET rate_multiplier = $1::numeric, updated_at = NOW()
+		WHERE id = $2 AND rate_multiplier = $3::numeric AND deleted_at IS NULL`, newGroupRate, groups[0].id, groups[0].rate)
+	if err != nil {
+		return err
+	}
+	affected, err := result.RowsAffected()
+	if err != nil {
+		return err
+	}
+	if affected != 1 {
+		return fmt.Errorf("upstream billing group rate CAS failed")
+	}
+	if err := enqueueSchedulerOutbox(ctx, client, service.SchedulerOutboxEventGroupChanged, nil, &groups[0].id, nil); err != nil {
+		return err
+	}
+	// Keep the audit record metadata-only: no account, credential, or model
+	// identifiers are persisted, and the request body remains empty.
+	detail, _ := json.Marshal(map[string]any{
+		"source":             "upstream_billing_probe",
+		"account_rate":       nextAccountRate,
+		"text_group_rate":    newGroupRate,
+		"rate_markup":        0.05,
+		"groups_updated":     1,
+		"manual_price_guard": true,
+	})
+	_, err = client.ExecContext(ctx, `
+		INSERT INTO audit_logs (created_at, actor_role, auth_method, action, method, path, request_body, status_code, latency_ms, extra)
+		VALUES (NOW(), 'system', 'internal', 'pricing.upstream_rate_sync', 'INTERNAL', '/internal/upstream-billing', '', 200, 0, $1::jsonb)`, string(detail))
+	return err
+}
+
 func lockAndMatchProbeProxyIdentity(ctx context.Context, client *dbent.Client, account *service.Account) (bool, error) {
 	if account.ProxyID == nil {
 		return true, nil
diff --git a/backend/internal/repository/account_repo_upstream_billing_probe_cas_test.go b/backend/internal/repository/account_repo_upstream_billing_probe_cas_test.go
index f9fe019..1bc1f23 100644
--- a/backend/internal/repository/account_repo_upstream_billing_probe_cas_test.go
+++ b/backend/internal/repository/account_repo_upstream_billing_probe_cas_test.go
@@ -101,6 +101,9 @@ func TestUpdateUpstreamBillingProbeSnapshotCommitsSnapshotAndOutboxAtomically(t
 	mock.ExpectExec(`(?s)`+regexp.QuoteMeta("UPDATE accounts")+`.*`+regexp.QuoteMeta("rate_multiplier = CASE")+`.*`+regexp.QuoteMeta("THEN $10::numeric")+`.*`+regexp.QuoteMeta("AND credentials = $5::jsonb")+`.*`+regexp.QuoteMeta("AND proxy_id IS NOT DISTINCT FROM $6")+`.*`+regexp.QuoteMeta("COALESCE(extra -> 'upstream_billing_probe', 'null'::jsonb) = $7::jsonb")).
 		WithArgs(sqlmock.AnyArg(), int64(17), service.PlatformOpenAI, service.AccountTypeAPIKey, `{"api_key":"sk-test"}`, nil, "null", "true", "true", 0.065).
 		WillReturnResult(sqlmock.NewResult(0, 1))
+	mock.ExpectQuery("SELECT g.id, g.rate_multiplier").
+		WithArgs(int64(17), service.PlatformOpenAI).
+		WillReturnRows(sqlmock.NewRows([]string{"id", "rate_multiplier"}))
 	mock.ExpectExec(regexp.QuoteMeta("INSERT INTO scheduler_outbox")).
 		WithArgs(service.SchedulerOutboxEventAccountChanged, int64(17), nil, nil, sqlmock.AnyArg()).
 		WillReturnResult(sqlmock.NewResult(1, 1))
@@ -130,6 +133,57 @@ func TestUpdateUpstreamBillingProbeSnapshotCommitsSnapshotAndOutboxAtomically(t
 	require.NoError(t, mock.ExpectationsWereMet())
 }
 
+func TestUpdateUpstreamBillingProbeSnapshotRepairsManagedGroupRateDrift(t *testing.T) {
+	db, mock, err := sqlmock.New()
+	require.NoError(t, err)
+	t.Cleanup(func() { _ = db.Close() })
+	driver := entsql.OpenDB(dialect.Postgres, db)
+	client := dbent.NewClient(dbent.Driver(driver))
+	t.Cleanup(func() { _ = client.Close() })
+
+	mock.ExpectBegin()
+	mock.ExpectExec(`(?s)`+regexp.QuoteMeta("UPDATE accounts")+`.*`+regexp.QuoteMeta("rate_multiplier = CASE")+`.*`+regexp.QuoteMeta("THEN $10::numeric")).
+		WithArgs(sqlmock.AnyArg(), int64(17), service.PlatformOpenAI, service.AccountTypeAPIKey, `{"api_key":"sk-test"}`, nil, "null", "true", "true", 0.5).
+		WillReturnResult(sqlmock.NewResult(0, 1))
+	mock.ExpectQuery("SELECT g.id, g.rate_multiplier").
+		WithArgs(int64(17), service.PlatformOpenAI).
+		WillReturnRows(sqlmock.NewRows([]string{"id", "rate_multiplier"}).AddRow(int64(31), 0.17))
+	mock.ExpectExec(`(?s)`+regexp.QuoteMeta("UPDATE groups SET rate_multiplier = $1::numeric, updated_at = NOW()")+`.*`+regexp.QuoteMeta("WHERE id = $2")+`.*`+regexp.QuoteMeta("AND rate_multiplier = $3::numeric")).
+		WithArgs(0.55, int64(31), 0.17).
+		WillReturnResult(sqlmock.NewResult(0, 1))
+	mock.ExpectExec(regexp.QuoteMeta("INSERT INTO scheduler_outbox")).
+		WithArgs(service.SchedulerOutboxEventGroupChanged, nil, int64(31), nil, sqlmock.AnyArg()).
+		WillReturnResult(sqlmock.NewResult(1, 1))
+	mock.ExpectExec(regexp.QuoteMeta("INSERT INTO audit_logs")).
+		WithArgs(sqlmock.AnyArg()).
+		WillReturnResult(sqlmock.NewResult(1, 1))
+	mock.ExpectExec(regexp.QuoteMeta("INSERT INTO scheduler_outbox")).
+		WithArgs(service.SchedulerOutboxEventAccountChanged, int64(17), nil, nil, sqlmock.AnyArg()).
+		WillReturnResult(sqlmock.NewResult(1, 1))
+	mock.ExpectCommit()
+
+	repo := newAccountRepositoryWithSQL(client, db, nil)
+	account := &service.Account{
+		ID:          17,
+		Platform:    service.PlatformOpenAI,
+		Type:        service.AccountTypeAPIKey,
+		Credentials: map[string]any{"api_key": "sk-test"},
+		Extra: map[string]any{
+			service.UpstreamBillingProbeEnabledExtraKey:    true,
+			service.UpstreamBillingRateSyncEnabledExtraKey: true,
+		},
+	}
+	nextRate := 0.5
+
+	err = repo.UpdateUpstreamBillingProbeSnapshot(
+		context.Background(), account,
+		&service.UpstreamBillingProbeSnapshot{Status: service.UpstreamBillingProbeStatusOK},
+		&nextRate,
+	)
+	require.NoError(t, err)
+	require.NoError(t, mock.ExpectationsWereMet())
+}
+
 func TestUpdateUpstreamBillingProbeSnapshotRejectsChangedProxyIdentity(t *testing.T) {
 	db, mock, err := sqlmock.New()
 	require.NoError(t, err)
@@ -178,6 +232,9 @@ func TestUpdateUpstreamBillingProbeSnapshotRollsBackWhenOutboxFails(t *testing.T
 	mock.ExpectExec(`(?s)`+regexp.QuoteMeta("UPDATE accounts")+`.*`+regexp.QuoteMeta("AND proxy_id IS NOT DISTINCT FROM $6")+`.*`+regexp.QuoteMeta("COALESCE(extra -> 'upstream_billing_probe', 'null'::jsonb) = $7::jsonb")).
 		WithArgs(sqlmock.AnyArg(), int64(18), service.PlatformOpenAI, service.AccountTypeAPIKey, `{"api_key":"sk-test"}`, nil, "null", "true", "true", 0.7).
 		WillReturnResult(sqlmock.NewResult(0, 1))
+	mock.ExpectQuery("SELECT g.id, g.rate_multiplier").
+		WithArgs(int64(18), service.PlatformOpenAI).
+		WillReturnRows(sqlmock.NewRows([]string{"id", "rate_multiplier"}))
 	mock.ExpectExec(regexp.QuoteMeta("INSERT INTO scheduler_outbox")).WillReturnError(errors.New("outbox failed"))
 	mock.ExpectRollback()
 
