# 项目空间清理与分支整合（2026-09-14）

本次按用户要求清理本地冗余，并将开始时获取到的全部本地、origin 分支历史整合到 main。
未启动部署、重装运行环境或发送业务消息。

## 空间结果

同一工作区的 `du -sk` 从 9,970,872 KiB 降至 4,802,800 KiB，约 **9.51 → 4.58 GiB**，净减少约 **4.93 GiB**。
磁盘可用空间从约 96 GiB 增至约 102 GiB；此数值也受项目外活动影响，释放量以工作区测量为准。

- 清除 Tauri `target` 编译缓存，约 2.21 GiB。
- 清除未运行的 New-API classic 前端依赖及构建产物，约 1.05 GiB；固定 gitlink、源码、`bun.lock` 均保留。
- 清除未运行的社交浏览器 `Cache` 和 `Code Cache`，约 1.26 GiB；Cookies、会话、站点存储和 Service Worker 数据保留。
- 前端检查完成后清除 manager 的 `node_modules`、`dist`，约 0.38 GiB；后续开发需在该目录执行 `npm ci`。
- 清除项目源码和测试的 Python 缓存；用 `git gc --no-prune` 压缩 Git 对象，保留恢复引用。
- 两套临时 npm 安装已删除，只保留检查日志。

删除前确认候选目录没有 Git 跟踪文件、不是符号链接且没有打开文件。
运行中的 Python venv、Gateway/npm 依赖、业务数据库、日志、凭据与备份均保留。
清理后的 Gateway、ClawBot、Intel listener PID 和启动时间仍与本轮开始时一致。

## 分支整合

共覆盖 14 个不同的非 main 分支名：展示分支 1 个、旧审计分支 2 个、Dependabot 分支 11 个。
旧审计分支此前已经合入；展示分支和依赖分支现在均为整合提交的祖先。

| 范围 | 最终处理 |
| --- | --- |
| GitHub 展示分支 | 纳入 README、工程导览与离线交互演示 |
| 4 个前端依赖分支 | 保留当前锁中相同或更高版本，避免旧锁覆盖新版本 |
| Gateway qs 分支 | 保留已集成的 6.16.0；运行安装目录未改 |
| 3 个 Python 分支 | 保留 pypdf 6.16.1、tornado 6.5.8、稳定版 RestrictedPython 8.3；没有降回预发布版 |
| OpenClaw vendor Hono / Sharp | manifest 分别更新至 4.13.5 / 0.35.4，同步供应链版本断言 |
| 微信 Vitest 分支 | 精确固定 Vitest 与 coverage-v8 5.0.0，重新解析锁文件，保留 Vite 7.3.5 和既有安全覆盖 |

使用正常合并提交保留分支历史，没有重写公共历史。
合并前各分支位置保存在本地 `refs/backup/maintenance-20260914/`，例如旧 main 为 `refs/backup/maintenance-20260914/heads/main`。
这些轻量引用没有上传，不是工作树副本。

## 验证与边界

- `make docs-check`：14 项测试及文档入口、布局、链接检查通过。
- 桌面、新闻、费用与操作边界：98 项 Node 测试通过。
- `npm run lint`、`npm run build` 通过；另用提交的桌面锁在临时目录完成 `npm ci`、类型、lint、build 与安全边界检查。
- `make python-lock-check`：Linux 与 macOS 哈希锁检查通过；本轮合并未改变现有 Python requirements 或锁文件。
- `make supply-chain-check`：2 个工作流、16 个 Action SHA、354 个受管 npm 包检查通过。
- 微信锁文件干净安装、Vitest CLI、34 个源文件语法转换通过；另以实际 AES 模块验证 Vitest 5、mocker 和 V8 覆盖率运行。
- Hono 内存请求、Sharp PNG 编解码检查通过；更新包组合与微信锁的 npm 审计均为 0 个漏洞。
- 本轮合并范围 Gitleaks 扫描通过；`git diff --check` 通过，子模块工作树干净。

未重新验收真实消息投递、账号操作或安装包运行；微信发布源码未附完整上游测试套件。
没有重建 Rust 原生缓存。远端 CI 以 main 对应提交的 Actions 状态为准。
