# OpenEverything 的 ChatGPT 协作

2026-09-09 已验证专用连接读访问、ChatGPT 规划与原会话恢复。
2026-09-10 回读本机绑定，原接入任务第 1 轮状态为 `DONE`；旧文档中“等待第 1 轮复核”
的检查点已过期。这里记录协作接入状态，不代表生产业务或消息投递在本轮重新验收。

工作区名为 OpenEverything，README 中的产品名称为 OpenClaw Bot。
ChatGPT 负责规划与独立审查，Codex 负责本地执行。唯一连接名为
`Codex with ChatGPT · OpenEverything`，不得转用其他项目连接。

## 日常使用

在本项目的 Codex 任务输入框中说：

> GPT：检查 OpenEverything 的 LLM 路由配置，先通过连接读取代码并规划，再由 Codex 执行必要修改和验证，最后独立复核。

直接在 ChatGPT 网页聊天可以规划和读取获准资料，不能自行唤醒本地执行。
无需复制原始日志或上传整个仓库。私有项目、聊天地址与工作区绑定只保存在本机。

## 项目范围

- 先读 README 和 `docs/current/current-baseline.md`。
- Python 后端在 `packages/clawbot`；从 `multi_main.py`、`src/llm_routing_config.py` 及相关业务源码进入。
- React、TypeScript、Tauri 和 Rust 桌面端在 `apps/openclaw-manager-src`；从 `package.json`、`src-tauri/Cargo.toml` 和所改模块进入。
- 协作读取范围排除运行状态、账户与生产数据；连接限制常见密钥文件及越界路径。执行输出只发布本次验证需要的脱敏结果。
- 接入配置本身不授权生产发布、消息发送、账户操作或付费；实际任务以用户当次授权和所属运行约束为准。

## 已有接入证据

接入时完成了工作区身份与连接健康检查，未授权请求被拒绝；ChatGPT 实际读取了
README、当前基线、前端依赖、LLM 路由配置和本说明，并补读后端入口与 API 源码。
它先列出再读取执行输出，提出仅涉及协作文档的计划。项目绑定保存回读、离开后重开
原会话均已验证。完整独立复核记录以原会话与本机检查点为准，不重复维护逐轮进度表。

接入时实际核对聊天模式与最高可用能力。账户记忆关闭；项目资料库只读显示启用，
指令禁止使用资料库及其他项目记忆；这不等于关闭了不可编辑的权限开关。

## 恢复

1. 在原 Codex 任务中要求继续，用已安装技能的 `c2c session` 回读本项目、原聊天和检查点，再运行 `c2c doctor`。
2. 有效连接直接复用；失效只修复本工作区。配对码、凭据及临时连接地址不提交 Git，不重绑其他项目。
3. 同一任务沿用原聊天并继续检查点，不重复 INIT；新 Codex 任务从已绑定 Project 新开聊天。核对实际地址与工作区身份后才能保存新绑定。
4. Codex 按计划修改并运行相关检查，用 `c2c record` 保存摘要和真实可读输出；ChatGPT 列出、读取执行记录及完整差异后独立复核。
5. 回读保存的绑定并验证原会话可恢复。只有复核完成后记录 `DONE` 并清除活动检查点。

具体安装位置、模型核对和浏览器操作遵循本机已安装的 Codex with ChatGPT 技能。
