# HANDOFF — 会话交接摘要

> 最后更新: 2026-08-08

---

## [2026-08-08 10:19] JIYU 生图与受管更新交接

### 本次完成了什么
- 生产新增渠道A/渠道B各一套 `gpt-image-2` 分组、账号、渠道和 300±30 秒监控；生图按上游单张价固定加 `0.05`，当前定价分别为 `0.10/张` 与 `0.12/张`。
- 本机重建 `jiyu-ai-image` stdio MCP，替换 CC Switch 两个失效旧条目并同步 Claude、Codex、OpenCode；Key 仅从 macOS 钥匙串或环境变量读取。
- 实现 JIYU 兼容包 CI、校验清单、root-only 更新代理、原子暂存和 WebUI 后端入口；暂存后由独立 systemd 任务验证运行哈希与健康状态并自动回滚。当前生产仍保持安全失败关闭，未直接放开官方二进制覆盖。
- 真实用户“渠道状态”复核发现仍混排；已在补丁中让用户卡片和管理表复用渠道A→渠道B排序器，待新构建上线后截图复验。

### 未完成的工作
- 渠道A真实生图返回 502，渠道B专用 Key 返回 401；需上游修复凭据/权限后再写入监控 Key、创建站内专用 Key并做一次真实单图验收。
- 受管更新需完成聚焦 CI 验证、发布首个兼容包、部署带新后端的生产构建，再启用 WebUI 按钮进行真实更新验收。
- 链动小铺仍需平台书面合规确认；7 档草稿保持下架/零库存，充值中心不回填不可购买链接。

### 需要注意的坑
- 不得把上游 Key、站内 Key、Cookie 或管理员凭据写入脚本、文档、截图、提交或 CI；MCP 安装状态可以记录布尔值，不能记录 Key。
- 生图付费 POST 禁止自动重试；上游 502/401 未解决前不得伪造正常监控或公开售卖。
- 不要直接解禁官方 update/rollback；WebUI 只允许调用无参数 root 代理并校验 JIYU 清单。

### 当前系统状态
- 生产仍为 `v0.1.172-jiyu.5`；10 个文本渠道和 2 个生图渠道均已建立，12 条监控合同为 300±30 秒。
- 本机生图 MCP 安装文件与 CC Switch 条目均存在，钥匙串尚无站内生图专用 Key，调用会明确失败关闭。
- 工作树仍在 `main`，最终聚焦验证、提交、推送和仅保留 `main` 尚未执行。

## [2026-08-08 08:25] JIYU v5、生图与自助更新交接

### 本次完成了什么
- 生产发布 `v0.1.172-jiyu.5`；真实 Chrome 复核监控固定为渠道A五条后接渠道B五条，渠道列只显示匿名渠道，真实错误/降级未被掩盖。
- “API 密钥”页和创建弹窗固定同时展示 Claude 根端点与 ChatGPT `/v1` 端点，CC Switch 导入仍默认开启。
- 确认 Sub2API 原生支持 Images API；确认本机两个旧生图 MCP 都指向不存在脚本，且两个现有上游配置只登记文本模型。
- 新增 `docs/053-jiyu-growth-payment-image-update-plan.md` 和 `docs/087-jiyu-image-mcp-guide.md`，给出生图专用 Key、MCP、链动充值和 WebUI 自助更新方案。

### 未完成的工作
- 需老板确认生图按倍率还是按张定价、是否只先开文生图单图、是否采用 CI 兼容包 + root-only 安装代理；确认前不改上游线路、不消耗生图余额。
- 链动小铺仍需平台书面合规确认，7 档草稿保持下架/零库存，充值中心没有购买链接。
- WebUI 目前能正确显示定制版状态，但还不能安全安装未来 JIYU 兼容包；官方更新接口继续失败关闭。

### 需要注意的坑
- 不要直接放开 Apache 的官方 update/rollback 路径；官方二进制会覆盖双端点、监控排序和 CC Switch 定制。
- 不要向 CC Switch 旧生图 MCP 条目填 Key；启动脚本已经不存在。
- 不要在生图模型、价格和协议未确认时猜名称或用真实余额试错。

### 当前系统状态
- Sub2API `v0.1.172-jiyu.5`、专用 Redis、更新检查 timer、每日备份 timer 均 active，内网健康检查通过。
- 10 个分组、10 个渠道、10 条 300±30 秒真实监控保持；新生图渠道尚未创建。
- GitHub 默认分支当前有 31 个 Dependabot 漏洞聚合提示（HI-997），尚未按生产路径分组处理。

## [2026-08-08 07:35] JIYU AI 生产审计与运营闭环交接

### 本次完成了什么
- 生产已发布 `v0.1.172-jiyu.3`：10 个分组、10 个渠道、10 条 300±30 秒真实监控；倍率合同均为上游绝对增加 `0.05x`，新建监控也默认 300±30 秒。
- 创建密钥页已区分 Claude/OpenAI 端点并默认导入 CC Switch；首页、标题、版本徽标和管理员菜单品牌残留已修复。
- 链动小铺资料和 7 档 JIYU 商品草稿已完成；平台合规禁止代理类服务，因此全部保持下架和零库存。
- 闲鱼 readiness v2、Sub2API 聚焦前端测试、类型检查、生产构建和热点 owner-loop 回归通过；截图保存在 `scripts/assets/`。

### 未完成的工作
- 链动小铺需取得平台书面合规确认后才能上架、完成 ¥1 真实购买并把链接回填充值中心。
- CC Switch 当前深链仍携带 Key；建议改为同域一次性票据，再制作默认开启用量查询和本地路由的 JIYU Switch。
- 自动发货仍暂停且兑换码库存为 0；真实监控仍有上游错误/降级，不具备公开售卖条件。
- 当前 Chrome 接管接口不能设置精确视口，本轮未重新生成 1440×1000/390×844 双主题全页面证据；生产 CSP 也需按 report-only 方案逐步收紧。

### 需要注意的坑
- 不要开启官方二进制自动覆盖；定制版发布只走 `install-jiyu-build`，失败自动回滚。
- 不要通过改名或暗语绕过链动小铺审核，不要把草稿链接写回充值中心。
- 不要把监控错误手工改绿；不要在文档、截图、日志或提交中保存密码、Key、Token、Cookie。

### 当前系统状态
- Sub2API、专用 Redis、更新检查 timer、每日备份 timer 均 active，生产健康检查通过。
- 链动小铺商品为 7 个下架草稿；充值中心仍不含链动购买链接。
- 工作树含本轮 JIYU/闲鱼改动，最终提交和只保留 `main` 尚未执行。

## [2026-05-04 12:00] Frist-API 清理提交前交接

### 本次完成了什么
- Frist-API 已完成 Workbench UI、New-API 桥接、DeepSeek v4 默认模型、登录免验证码、注册挑战增强、CC Switch/Codex 导入和美元计价等前序改动。
- 本轮补齐后端不可用时的用户恢复入口：工作台会显示离线恢复条，并提供一键重新连接。
- 文档治理已归集到 `docs/` 根目录编号文件，旧散落文档和历史 usecase 文档已进入本次清理范围。

### 未完成的工作
- 正式商业化仍需固定域名 HTTPS、真实支付回调、注册邮箱验证码/找回密码闭环、Turnstile/Redis 限流、管理员 2FA、数据库迁移、备份监控和真实 DeepSeek Key 在 Codex 桌面端端到端实测。
- New-API 已作为上游 submodule 和桥接层接入，但历史用户、余额、Key、订单和日志迁移仍需单独演练。

### 需要注意的坑
- 不要把服务器密码、管理员入口码、管理员令牌、用户真实 Key、上游 Key 或运行时 JSON 写入仓库、文档或最终汇报。
- `packages/new-api-upstream` 是上游 submodule，同步应走 `make new-api-check` / `make new-api-sync`，不要手工复制上游代码。

### 当前系统状态
- Frist-API 本地测试需要继续以 `cd apps/frist-api && npm test` 作为提交前验收。
- New-API 同步检查需要继续以 `make new-api-check` 验证 GitHub latest release、submodule 指针和 Compose 镜像一致。
