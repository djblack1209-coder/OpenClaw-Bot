# HANDOFF — 会话交接摘要

> 最后更新: 2026-08-10

## [2026-08-10] 链动小铺七档兑换码库存补齐

- 用户明确要求每个现有面值新增 100 张，并上架链动小铺；实际面值为 ¥1/10/50/100/300/500/1000。
- 生产数据库七档均为 101 张 `unused`，链动小铺逐项回读均为 101 张、状态“销售中”，总库存 707 张。新增过程使用生产前备份、浏览器登录态和内存中转，未把卡密写入本地文件、命令输出、仓库或文档。
- 未创建订单、支付、测试用户或新的事实源；后续首笔真实购买仍需按既有小额单验收流程人工确认。

## [2026-08-10] 新增 ¥30 兑换码商品

- 用户随后要求新增 ¥30 面值；生产预写备份后生成 100 张，数据库回读为 100 张 `unused`，链动小铺唯一 ¥30 商品回读为 100 张、状态“销售中”。
- 初次复制商品模板产生一个空副本，已立即删除空副本；保留商品的标题、售价和公开详情均修正为 ¥30。当前八档总库存为 807 张，未创建订单、支付或测试用户。

## [2026-08-10] 用户要求撤回 Channel Monitor V2

- 用户认为 V2 被动流量视图不符合使用预期，已通过 Sub2API 原生系统设置切回 V1 主动探测：监控启用、默认间隔 60 秒。
- 这是用户明确的产品选择，不是稳定性阻塞项。V2 配置、watermark、聚合表和历史记录均保留，后续维护窗口可恢复；没有删除渠道或新增自建监控。
- 切换后生产回读为 `channel_monitor_mode=v1`，近 5 分钟有 4 条 V1 历史检查更新；Sub2API、Apache 和健康端点正常。

## [2026-08-10] 中国生产面临时暂停，海外面保持唯一生产入口

- 已在最新 Sub2API 完整备份后，通过生产管理器原子启用 Apache 中国生产闸门。可信 `CN` 请求在站点入口返回 403，境外请求返回 200；服务健康、Responses WebSocket、备份和更新定时器保持正常。
- 国内 DeepSeek、Kimi、SiliconFlow、智谱账号、分组、渠道和精确模型白名单仍保留在 Oracle 数据库中，没有删除、停用或复制到其他服务器；恢复前必须安排维护窗口并运行 `cn-production resume`，再做真实双地域验收。
- Channel Monitor V2 是 Sub2API v0.1.173 原生监控核心（设置、调度、watermark、聚合和指标表均为上游能力）。JIYU 仅保留设置页接线、供应商标签/排序和只读 readiness 审计，不存在第二套监控引擎。
- 当前海外生产探针：CN/SG 源站标记回读为 403/200；就绪审计回读 18 组、16 账号、16 活动渠道，倍率合同正确；V2 watermark 和聚合指标有最新更新时间。

## [2026-08-10] JIYU v0.1.173 与被动监控生产闭环

- 生产已升级为 `v0.1.173-jiyu.31344140382`；发布前备份、健康失败回滚、ARM64 哈希、主线 CI 和兼容工作流均通过。
- Channel Monitor V2 已启用；旧主动监控记录保留但不再运行。真实回读显示旧 `last_checked_at` 无新增，v2 watermark/聚合器正常。
- 四家国内 `/models` 零费用探针仍为 200，模型映射计数为 2/4/8/61；地域回读 CN/SG/未知为 403/200/403。一次 `/v1/usage` 只读探针更新了 `last_used_at`，已按 prestate 恢复，后续不得把该端点当作无副作用探针。
- Prompt Audit 仍保持默认关闭（无配置行、无节点），因为现有 Key 没有专用安全分类模型；原生内容预拦截仍为 29 条规则、Unicode NFKC、30/1 天留存。

## [2026-08-10] 中国/海外站点级分流目标修正

- 新产品目标：大陆 IP → 中国服务器站点，其他 IP → Oracle Singapore 海外站点；Oracle 继续唯一保存账户、余额、订单、API 密钥和账本。
- 当前未闭环：现有中国主机没有 JIYU 服务/vhost、没有全局 IPv6，443 和防火墙属于共享业务边界；腾讯 SSH 不可达，没有 JIYU 中国域名/ICP/独立健康端点；不要修改共享中国主机或把其他项目服务当作 JIYU origin。
- Cloudflare 官方门槛已核对：Load Balancing 是付费附加项；China Network 需要 Enterprise、单独订阅、ICP/许可和 JD Cloud 内容审核。项目不允许擅自升级付费套餐，故本轮只记录设计，不部署 Worker、DNS、Cloudflare 路由或中国服务。
- 待用户处理：确认中国 origin 所属项目、域名和 ICP/许可；确认是否批准 Cloudflare 付费/Enterprise，或批准一次性评估 Worker 按 `request.cf.country` 代理方案；提供可回读的中国 origin 健康端点和回滚权限。

## [2026-08-10] Cloudflare 控制台只读复核

- 已通过现有 Chrome 登录态进入 `245334.xyz` 控制台；区域为 Business 套餐，当前 JIYU 的代理 DNS 记录只指向 Oracle Singapore，没有中国 JIYU origin 或现成地域路由。
- 这消除了“Cloudflare 浏览器无法接管”的旧前置，不改变 China Network/Load Balancing 的套餐与合规门槛，也不授权修改 DNS、Worker 或共享中国主机。

---

## [2026-08-09] JIYU 地区分流实施前置已核实

### 本次完成了什么
- 已只读确认 Cloudflare API 可用，当前生产区域没有指向 JIYU 的 Worker 路由；既有订阅回退 Worker 与 JIYU 无关，不得复用。
- 已核对当前 Sub2API 网关：除 OpenAI 兼容目录外，还存在 Gemini 原生、Codex 与 Antigravity 的独立模型目录和请求入口。地区规则必须同时约束目录与带模型的请求，不能只隐藏前端或单个 `/v1/models` 端点。
- 已只读核对 12 条活动业务渠道：当前可调度模型均非国内模型，国内模型允许集为空。

### 未完成的工作
- 未部署 Cloudflare Worker、路由或源站代码。当前若启用“中国大陆仅见国内模型”，会将大陆目录过滤为空，无法实现已公布的产品规则。
- 需先由资产所有者配置真实可用的国内模型，并确认精确模型允许集；随后才可在隔离的干净源范围内实现最小 Cloudflare 地区标记和海外主系统的统一目录/请求策略。
- 上线前仍需重新获取 Cloudflare Worker/路由 prestate、导出可恢复备份、准备回滚，并以真实大陆和境外访问验证模型不泄漏且账户余额不分裂。

### 需要注意的坑
- 旧 First-API 域名别名仍有外部响应，尚无“无生产调用者”证据，不能作为本轮清理对象直接删除。
- 用户在聊天中给出的任何上游密钥均视为已暴露；应由用户在上游轮换，且不得写入仓库、文档或受保护配置以外的位置。

### 当前系统状态
- Cloudflare 权限已满足只读审计与后续最小写入准备，但地区技术分流仍未上线；支付、对象存储和异步生图继续保持关闭或待实施。

---

## [2026-08-09] JIYU 地区条款生产文案修正

### 本次完成了什么
- 在新的 Sub2API 一致性备份和单独条款/文档/管理脚本恢复副本之后，生产登录前条款与受保护文档源已移除“中国大陆整体不开放”的旧表述。
- 条款改为：中国大陆 IP 仅显示并可使用国内模型，境外 IP 显示全部模型；账户、余额、订单、API 密钥和模型请求仍由海外主系统统一处理。页面明确技术分流尚未完成真实回读。
- 写入后 `sub2api.service`、专用 Redis、Apache 均 active，回环健康 200；公网未登录页回读新条款且未命中旧文案。

### 未完成的工作
- Cloudflare 域名级只读权限与当前 Worker 路由范围已确认；服务端目录过滤和中国大陆、境外真实页面/API 回读尚未实施。当前没有可用国内模型允许集，不得把文案修正写成地区分流已上线。
- 受保护文档页需用户自行阅读并同意条款后才能完成真实渲染回读；不得代用户同意。

### 需要注意的坑
- 回滚材料位于最新 Sub2API 备份中，仅用于恢复管理脚本、文档页和条款 JSON；不要复制或提交其中的环境文件或数据库内容。
- 用户此前在聊天中提供的国内上游密钥应视为已暴露，需由用户在对应上游轮换；不得复述、写入或使用该值。

### 当前系统状态
- 支付、对象存储、异步生图和地区技术分流仍保持关闭或待实施；12 条业务渠道继续活跃，两个自营 Plus/Pro 池仍无库存、成员、渠道和监控。

## [2026-08-09] 闲鱼桌面入口移除扩展前置

### 本次完成了什么
- 闲鱼启动器只打开独立卖家浏览器和回环 CDP，不再复制扩展、读取 `.env`、写入运行配置、打印 Token，且旧 `--copy-token` 继续失败关闭。
- OpenClaw 桌面端发现逻辑不再要求扩展清单；操作台改为提示点击“启动并打开运营台”并在隔离窗口首次登录闲鱼。
- X/小红书的 Social Pilot 检测和能力上报保留，`manifest_version=bridge` 仍表示本机卖家桥接器能力。

### 未完成的工作
- 尚未运行本机重新安装；只需在当前维护窗口做聚焦验证，不要重新执行 `make tauri-build`。
- 闲鱼首次登录和真实订单履约仍需用户在隔离浏览器中完成，未读取或记录 Cookie。

### 需要注意的坑
- 不要再提示用户运行命令、打开 `chrome://extensions` 或粘贴 Token 来使用闲鱼入口；闲鱼与 X/小红书社媒入口是两条独立能力链。

### 当前系统状态
- 本次只修改本地源码、聚焦测试和当前文档，未触碰生产、SSH、Cloudflare、账户或已安装 App，未提交。

## [2026-08-09] 闲鱼运营状态提示同步桌面端入口

### 本次完成了什么
- 状态投影的补救、断线、登录和 webhook 配置提示已统一指向 OpenClaw 桌面端运营台，不再要求刷新旧 Chrome 插件、打开插件弹窗或执行重启命令。
- 保留首次使用边界：用户仍需在隔离卖家浏览器中人工完成闲鱼首次登录，之后由本机回环 CDP 接管。

### 未完成的工作
- 真实闲鱼首次登录和真实订单履约仍需用户在隔离浏览器中完成；本轮未启动服务、未触碰生产或账户。

### 需要注意的坑
- 不要把历史 CHANGELOG 中的 Chrome 插件看守步骤当作当前入口；当前入口是 OpenClaw 桌面端按钮。

### 当前系统状态
- 聚焦投影测试 3 项通过，Python 编译和 `git diff --check` 通过；本地工作树未提交。

## [2026-08-09] 闲鱼桌面入口自动启动托管服务

### 本次完成了什么
- Tauri “启动并打开运营台”按钮现在先幂等复用 `ai.openclaw.xianyu` 托管服务启动路径；已运行时不重启，然后继续打开隔离卖家浏览器和一次性本机运营台。
- 未增加依赖、常驻服务、控制面或凭据输入；工作树其他改动保持不动。

### 未完成的工作
- 尚未重新构建或安装桌面 App，也未做真实按钮点击；这需要下一次明确桌面维护窗口。

### 需要注意的坑
- 启动链接仍只允许本机回环、一次消费且短时有效；首次闲鱼登录仍由用户在隔离浏览器中完成。

### 当前系统状态
- `cargo test --locked xianyu`、`cargo fmt -- --check`、`npx tsc --noEmit` 与 `git diff --check` 通过；本次未触碰生产、Cloudflare、账户或支付，未提交。

## [2026-08-09] 闲鱼旧 QR 凭据入口已移除

### 本次完成了什么
- `make cc-seller-chrome` 不再默认传入 `--copy-token`；遗留参数会失败关闭，启动器不会复制 Token 到剪贴板。
- 已删除无生产调用者的闲鱼 QR API 路由、桌面 API 封装、`qr_login.py` 和 Telegram QR 通知链。
- 当前登录入口仅为隔离卖家 Chromium 与 OpenClaw 桌面运营台；`xianyu_live` 的正常浏览器登录、CookieCloud 和运行态 Cookie 读取保持不变。

### 未完成的工作
- `xianyu_live` 的 `.env` 自动浏览器登录/刷新边界仍待独立维护窗口评估，不能用旧 QR、剪贴板或 Telegram 会话链回退。

### 需要注意的坑
- 不要复制、导出、粘贴、聊天转发或截图 Cookie、Token 和会话；不要触碰生产、SSH、VPS、Cloudflare、支付、账户或 MFA。

### 当前系统状态
- 本轮仅处理本地旧入口和文档，未读取凭据、未启动服务、未连接生产，未提交。

---

## [2026-08-09] Telegram 本地补号批次控制已移除

### 本次完成了什么
- `/jiyu_replenish` 现只说明远程材料提交和远程批次控制均已禁用，并唯一指向本机 `make jiyu-sub2-replenish`。
- 已删除 Telegram 侧独立 `ReplenishRunner`、状态读取、停止调用与补号键盘；`status`、`stop` 和遗留键盘操作只回复无法查看或控制本地批次。
- 提示后的下一条普通私聊消息继续被一次性保护消费而不读取；只有 `/jiyu_replenish cancel` 可取消等待状态。

### 未完成的工作
- 未发送真实 Telegram 命令，也未重载本地 Bot；外部命令响应保持未验证。

### 需要注意的坑
- 不要将 Telegram 的状态、停止或旧键盘提示当作本机补号批次的观察或控制入口。

### 当前系统状态
- 本轮只修改本地源码、聚焦测试和当前记录，未连接生产或读取凭据。

---

## [2026-08-09 07:20] Telegram 补号材料入口已关闭，本地 Bot 已重载

### 本次完成了什么
- 该历史记录中的状态/停止表述已由当前代码收口：`/jiyu_replenish` 现在只提示本机 UI、取消保护等待和一次性保护消费，不构造、查看或停止本机 Runner。
- 当前运维口径已明确所有敏感补号材料只能进入本机可信 UI `make jiyu-sub2-replenish`。
- 当前交接记录不保留敏感标识或内部定价公式。

### 未完成的工作
- 浏览器没有认证的 Telegram Web 会话，因此未发送真实 Telegram 命令；外部命令响应仍未验证。

### 需要注意的坑
- 任何账户材料不得进入 Telegram、群聊、普通聊天或工单；不要把测试、历史文档或旧菜单当作远程提交授权。
- 不得将本地重载或未发送的 Telegram 命令表述为生产端到端 Telegram 命令测试通过。

### 当前系统状态
- 本地历史重载记录保留；当前代码安全边界已收口，Telegram 外部命令响应仍未验证。

## [2026-08-09 07:10] JIYU 全量审计文档与生产阻塞

### 本次完成了什么
- 020-029 文档已完成，包括本批新增指南；服务条款仍为待发布草案；本轮未产生生产变更。

### 未完成的工作
- 公开法律页面和地域规则仍未修改；支付、S3/异步生图保持关闭；自有 Plus/Pro 当前成员、渠道、监控均为零。

### 需要注意的坑
- JIYU/中转管理凭据视为已暴露，任何写入前必须轮换；不要把秘密写入文档或聊天。

### 当前系统状态
- 公网 `/health` 和模型广场只读检查已通过；公开服务条款仍为旧版本；流量分流尚未部署。

## [2026-08-09 06:50] JIYU 自营号池库存待补审计

### 本次完成了什么
- 只读就绪审计现在单独统计 Plus/Pro 自营号池的账号成员、活动渠道和监控，并输出库存、渠道、监控及整体运行就绪布尔值。
- 当前两个合规分组外壳均为零账号成员、零活动渠道、零监控，审计明确报告“库存待补”；公开售卖渠道的原有合同不因此失败。

### 未完成的工作
- 自营号源导入、渠道绑定和监控启用仍必须由资产所有者在实际补号时确认；本轮未进行任何生产写入。

### 需要注意的坑
- 不要把“库存待补”当作当前公开售卖渠道故障，也不要通过审计脚本补号、建渠道或启用监控。
- 审计输出只保留计数和布尔值，不记录凭据或账户标识。

### 当前系统状态
- 仅本地只读审计脚本与文档变更；未改动 SSH 目标、Cloudflare、服务设置、生产数据或凭据。

---

## [2026-08-09 06:45] Sonic 隔离期间 VPS 运维命令安全边界

### 本次完成了什么
- 在 VPS-Config 唯一当前基线和新会话提示词中写明：当前 Sonic 专用维护隔离窗口未结束时，`/scan`、`/repair`、`/manual` 不得发起 broad 或 Sonic 目标的 `vpsctl pull/status/diff`、探针或修复。
- 缺少明确主机和最小范围时必须失败关闭；只有用户明确重新开启维护窗口后，才允许指定主机的目标化操作，并继续使用 fresh prestate、备份、回滚和真实业务探针。

### 未完成的工作
- 运行时 Telegram prompt 仍可能在 `/scan` 路径隐含调用 broad `vpsctl`；当前 Sonic 隔离窗口内不得修改脚本或服务。窗口开启后，技术支持需先读取最新 prestate，再做目标化最小修复并完成真实探针。

### 需要注意的坑
- 当前任务禁止使用 VPS Bot 的 `/scan`、`/repair`、`/manual` 触发任何 broad 或 Sonic 操作；不要运行 `vpsctl` 验证文档。
- 生产运行时优先；不得把本地文档、旧报告或模拟结果当成生产结论，也不得记录密码、Token、Cookie、私钥、订阅地址或账户标识。

### 当前系统状态
- 本轮只改文档，未连接 Sonic、未运行 `vpsctl`、未修改生产脚本/服务；`git diff --check` 为唯一必要验证。

---

## [2026-08-09 06:44] 消息链路测试夹具异步合同修复

### 本次完成了什么
- 基线提交已复现端到端消息链路失败：测试替身把被 `await` 的补号入口保留为同步 `MagicMock`，导致普通中文命令分派提前返回。
- 测试工厂现显式提供返回 `False` 的 `AsyncMock`，与真实补号协程的“未等待号源时交给普通对话”合同一致；未修改生产消息处理。

### 未完成的工作
- 已安装 Tauri 管理器仍未获得真实界面核验，不能据此删除旧 New-API 兼容链或更改生产配置。

### 需要注意的坑
- 为被 `await` 的生产接口编写测试替身时必须使用 `AsyncMock`；同步 `MagicMock` 会掩盖后续业务分派。
- 生产、浏览器和测试日志继续禁止输出密码、API Key、Token、Cookie、TOTP secret、卡密或账户标识。

### 当前系统状态
- 本次仅修复既有测试夹具；单测、完整 `make test` 与 `git diff --check` 均已通过，无生产写入。

---
## [2026-08-09 10:30] 闲鱼桌面端一键运营入口

### 本次完成了什么
- OpenClaw Tauri 闲鱼页面的“启动并打开运营台”按钮会调用现有仓库启动器自动打开隔离卖家 Chromium，再复用本机 `127.0.0.1:18800` 的一次性启动交换进入 `/dashboard`；不新增 Cookie/Token 输入。启动器/Node 或仓库运行文件缺失时失败关闭并显示错误。
- `xianyu_admin` 新增仅回环可用的桌面启动交换：现有 API Token 只在本机请求头验证；返回的 nonce 约 60 秒有效且只能消费一次，消费后设置已有 15 分钟 HttpOnly 管理会话并 303 到 `/dashboard`。
- 桌面端在读取 API Token 前严格验证 `XIANYU_ADMIN_URL`：只允许无路径/凭据的本机回环 HTTP 地址；远程、HTTPS、伪造后缀主机、路径/查询或缺失主机均不发请求，端口可继续配置。
- 桌面页移除 Telegram 二维码登录弹窗；资产所有者登录闲鱼仍应使用隔离卖家 Chromium。

### 未完成的工作
- 未做生产部署、LaunchAgent 重载、Cloudflare/账户/支付/权限变更；未触碰 Sonic 专用维护窗口。
- 旧 `--copy-token` 剪贴板副作用和 `qr_login.py` 会话落盘链已移除；仅保留 `xianyu_live` 的 `.env` 自动浏览器登录/刷新边界待独立维护窗口评估。

### 需要注意的坑
- 启动链接只在本机回环地址有效，过期或重复访问返回 404；不要复制、截图或记录链接。
- 浏览器中的 Cookie 状态只显示健康摘要，不代表可以导出或手工转移登录态。

### 当前系统状态
- 已识别一次真实 `make tauri-build` 曾在构建中使 `/Applications/OpenClaw.app` 暂时缺失；当时已从严格签名工件和既有回滚副本恢复，未记录可归因的中断原因。随后仅本地修改安装脚本：旧 App 将保留至候选包构建、签名和临时副本校验通过，之后才同目录交换并清理 legacy App；跨 worktree 删除和 Dock/Launchpad 副作用已移除。
- 本次安装器修复只完成 `bash -n`、ShellCheck、现有失败前备份回归、`git diff --check` 和文档结构检查；没有再次运行 `make tauri-build`，没有触碰 `/Applications`。下一次明确桌面维护窗口应观察真实构建；SIGKILL、断电或文件系统故障发生在两次 rename 之间，仍需要依赖 `.previous-*` 暂存与持久回滚副本人工恢复。
- 此前 URL 守卫聚焦回归、`cargo test --locked` 49/49、`cargo check --locked` 和本机 18800 dashboard/ops-links 复验记录保持有效；桌面自动化服务不可用，闲鱼按钮 UI 点击及浏览器地址复验待该服务恢复后再做。共享工作树仍包含其他未提交改动，提交前必须逐文件审查。
